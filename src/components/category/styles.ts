import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    paddingLeft: 10,
    flexDirection: 'row',
    alignItems: 'center',
    gap: 5
  },
  name:{
    fontSize: 16,
    fontWeight: '600'
  }
})